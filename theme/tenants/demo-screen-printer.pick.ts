import type { DeepPartial, ThemeTokens } from '../../client/src/types/theme-tokens';

/** L3 curated picks — empty until Phase 6 client customization. */
export const pick: DeepPartial<ThemeTokens> = {};
