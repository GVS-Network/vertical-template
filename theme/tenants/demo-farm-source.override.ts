import type { DeepPartial, ThemeTokens } from '../../client/src/types/theme-tokens';

/** L4 leaf-only overrides — empty until Phase 6 client customization. */
export const override: DeepPartial<ThemeTokens> = {};
